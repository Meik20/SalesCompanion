[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["cd server && npm ci --omit=dev --no-audit --no-fund"]

[phases.build]
cmds = []

[start]
cmd = "cd server && node server-firestore.js"
